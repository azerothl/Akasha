[package]
name = "akasha-rag"
version.workspace = true
edition = "2021"
description = "RAG pack: spec + runbooks indexing and retrieval for Akasha Core Model"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
thiserror = "1.0"
