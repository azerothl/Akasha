[package]
name = "akasha-plugin-api"
version = "0.1.0"
edition = "2021"
description = "Plugin API traits and types (channel, tool, skill, memory, model, security)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
toml = "0.8"
anyhow = "1.0"
