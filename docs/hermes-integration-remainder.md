# Hermes parity — remainder (roadmap)

Items from the integration plan **not** fully implemented in this iteration (PTy, advanced Docker ops, full webhook platform, MCP OAuth, lifecycle hooks sandbox, etc.) remain **roadmap**. Track them in the issue tracker and in `docs/hermes-akasha-parity-matrix.md`.

Satellite repos (`Akasha_skills`, `Akasha_plugins`, `akasha-code-studio`, `Rbitnet`) : incremental PRs per repo as product priorities allow.
